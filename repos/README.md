# Repository checkouts

This directory contains the ecosystem repositories as Git submodules in the
actual Git repository.

The ZIP snapshot does not contain Git submodule gitlinks. Run the root
`bootstrap.ps1` or `bootstrap.sh` after creating the repository to register and
initialize them from `.gitmodules`.
